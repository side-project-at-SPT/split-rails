<div>
  <h1 class="font-bold text-4xl">Home#index</h1>
  <p>Find me in app/views/home/index.html.erb</p>
  <% if current_user %>
    <p>Hi, <%= current_user.name %></p>
    <%= link_to '房間列表', rooms_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to '遊戲列表', games_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <div class="mt-10">
      <h1>h1</h1>
      <h1>h1</h1>
    </div>
  <% else %>
    <div class="mt-5">
      <%= link_to 'Login', new_user_session_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white font-medium" %>
    </div>
  <% end %>
</div>
